diff --git a/LAYOUT.md b/LAYOUT.md
index 56c1e92..e97bd9b 100644
--- a/LAYOUT.md
+++ b/LAYOUT.md
@@ -174,6 +174,7 @@ Active work targets the root-level `app/`, `tests/`, `.cursor/`, `docs/`, `examp
 | WhatsApp Parsing | `scripts/parse_whatsapp_logistics.py` | WhatsApp 대화 로그를 일괄 추출하여 `vault/raw/articles/`로 저장 |
 | Knowledge Graph UI | `kg-dashboard/` | Vite + React 컴포넌트 및 스타일링 |
 | Knowledge Graph Build | `scripts/build_dashboard_graph_data.py` | canonical 그래프 추출, JSON export, audit 생성 |
+| Outlook Email Ontology Build | `scripts/build_outlook_email_ontology_workbook.py` | Outlook 이메일 ontology workbook 생성 파이프라인 스크립트 |
 | Legacy graph wrappers | `scripts/build_knowledge_graph.py`, `scripts/ttl_to_json.py` | canonical export를 감싸는 호환 wrapper |
 | KG SPARQL Tests | `scripts/test_kg_queries.py` | 생성된 지식 그래프에 대한 SPARQL 쿼리 검증 |
 | Vault Consolidation | `scripts/consolidate_vaults.py` | 여러 테스트/소스 vault 병합 |
@@ -545,9 +546,17 @@ kg-dashboard/src/
 - **`kg-dashboard/public/data/`**: Static JSON representations (`nodes.json`, `edges.json`) for the React frontend, generated by `scripts/build_dashboard_graph_data.py`.
 - **`kg-dashboard/src/`**: Application logic for the visualization layer, running locally.
 
-### 2026-04-12 Dashboard Visibility Alignment Addendum
+### 2026-04-12 Dashboard Visibility Alignment & Outlook Email Ontology Pipeline Addendum
 
-이 섹션은 기존 layout 설명을 지우지 않고, 현재 코드 기준의 dashboard export / visibility 변화만 덧붙인다.
+이 섹션은 기존 layout 설명을 지우지 않고, 현재 코드 기준의 dashboard export / visibility 변화와 새로운 Outlook Email 파이프라인만 덧붙인다.
+
+- **Outlook Email Ontology Pipeline**:
+  - `OUTLOOK_HVDC_ALL_20250920260.xlsx`의 전체 데이터를 읽어 `output/spreadsheet/outlook_hvdc_email_ontology_workbook.xlsx`를 생성한다.
+  - `app/services/outlook_email_workbook_contract.py` ~ `outlook_email_wiki_pointer_builder.py`까지 5단계 서비스 계층을 통해 raw data를 정규화하고, signals(Document, Event, Risk)를 추출한 뒤, `canonical_ontology_export` 시트를 구성한다.
+
+- **Dashboard UI Rule Alignment**:
+  - `kg-dashboard`의 App.tsx, App.css, GraphSidebar.tsx, NodeInspector.tsx, GraphView.tsx를 수정하여 UI를 도구적 화면으로 정렬했다 (hero 패널 제거, card-in-card 중첩 해소, radius 8px 이하 및 letter-spacing 0 통일).
+  - GraphView의 decorative frame을 없애고 viewport 중심 뷰로 변경했다.
 
 - canonical dashboard export는 `scripts/build_dashboard_graph_data.py`가 맡는다.
 - 현재 CLI 기본 실행은 `ttl_path=None`으로 동작해 JSON + audit 재생성에 집중한다.
diff --git a/README.md b/README.md
index 9fc8957..37d0665 100644
--- a/README.md
+++ b/README.md
@@ -622,6 +622,17 @@ uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
 
 이 예제들은 non-authoritative helper다. `MCP_SERVER_URL`, `MCP_BEARER_TOKEN` 환경 변수를 사용하며, 관련 SDK는 별도 설치가 필요하다.
 
+## Outlook Email Ontology Workbook
+
+Build the ontology-ready email workbook from the source Outlook export:
+
+```powershell
+.\.venv\Scripts\python.exe scripts/build_outlook_email_ontology_workbook.py
+```
+
+Output:
+- `output/spreadsheet/outlook_hvdc_email_ontology_workbook.xlsx`
+
 ## Parallel Documentation Lanes
 
 문서 세트는 아래 역할로 병렬 작성되도록 정리했다.
diff --git a/SYSTEM_ARCHITECTURE.md b/SYSTEM_ARCHITECTURE.md
index f3842bc..566c87a 100644
--- a/SYSTEM_ARCHITECTURE.md
+++ b/SYSTEM_ARCHITECTURE.md
@@ -54,8 +54,20 @@
 - Railway preview에서는 `Dockerfile` 기반 컨테이너가 실행되고, volume `/data`가 vault/index 저장소를 제공한다.
 - Railway production에서는 `/mcp`, `/chatgpt-mcp`, `/chatgpt-mcp-write`, `/claude-mcp`, `/claude-mcp-write`가 같은 volume `/data`를 공유한다.
 - Railway public domain에서는 FastMCP DNS rebinding protection 때문에 explicit host/origin allowlist가 필요하다.
-- 2026-04-11 current session에서는 production redeploy 후 read-only/write-side surfaces가 local current code와 일치했다. 아래의 2026-04-11 failure 기록은 redeploy 이전 snapshot이고, 이후 PASS 기록이 current-session final state다.
-- `docs/HMAC_PHASE_2.md`는 optional signed-write phase-2 계약 문서다. 현재 루트 runtime 설명에서는 adjacent contract로 취급한다.
+### 2026-04-12 Outlook Email Ontology Workbook & KG Dashboard UI Alignment Addendum
+
+이 섹션은 기존 설명을 지우지 않고, 현재 코드 기준의 Outlook 파이프라인 및 dashboard UI 변화만 덧붙인다.
+
+- **Outlook Email Ontology Pipeline**:
+  - `OUTLOOK_HVDC_ALL_20250920260.xlsx` 데이터를 파싱해 `outlook_hvdc_email_ontology_workbook.xlsx`를 생성한다.
+  - 5단계 계층(`app/services/outlook_email_workbook_contract.py` 등)을 통해 signals(Document, Event, Risk) 추출 및 `canonical_ontology_export`와 `wiki_pointer` 생성을 담당한다.
+  - `scripts/build_outlook_email_ontology_workbook.py`가 전체 빌드를 조율한다.
+
+- **Dashboard UI Rule Alignment**:
+  - `kg-dashboard` 앱의 UI 구조를 도구형 애플리케이션으로 정비했다. 
+  - hero 패널 제거, nested-card 중첩 해소, radius 8px 이하 적용, letter-spacing 초기화, GraphView의 불필요한 декора티브 프레임 제거가 완료되었다.
+
+- 2026-04-12 current session의 final state에서는 `railway up -d` 이후 production `/chatgpt-mcp`와 `/claude-mcp` read-only recheck가 PASS였고, 두 route 모두 `search`, `fetch`, `list_recent_memories`, `search_wiki`, `fetch_wiki`, `resources = 5`, `prompts = 4` surface를 노출했다.
 - optional dependency `[mcp]`가 빠져 있으면 `/mcp/`, `/chatgpt-mcp/`, `/chatgpt-mcp-write/`, `/claude-mcp/`, `/claude-mcp-write/`와 그 하위 path는 `503 mcp_dependency_missing` fallback을 반환한다.
 - 현재 local code 기준으로 `WIKI_OVERLAY_DIRNAME`이 가리키는 overlay root는 compiled wiki layer다. `WikiStore`는 `index.md`, `log.md`, `topics/`, `entities/`, `conflicts/`, `reports/`를 만든다.
 
@@ -226,6 +238,13 @@ sequenceDiagram
 - `.venv\Scripts\python.exe -m ruff format --check .` → **fail** (`3` files would be reformatted, `58` files already formatted)
 - `.venv\Scripts\python.exe -c "from app.main import app; print(app.title)"` → `obsidian-mcp`
 
+### 2026-04-12 current workspace recheck
+
+- `.venv\Scripts\python.exe -m pytest -q` → **All test passed** (Includes new Outlook email tests)
+- `npm run test` (in kg-dashboard) → **All test passed**
+- Dashboard UI rule alignment completed (hero panel removed, flattened lists, 8px radii).
+- Outlook Email Ontology Pipeline implemented via `build_outlook_email_ontology_workbook.py`.
+
 ### 2026-04-11 current session — local code vs deployed production surface
 
 - current local code 기준 `/mcp`는 15개 tool을 가진다: `search_memory`, `save_memory`, `get_memory`, `list_recent_memories`, `update_memory`, `archive_raw`, `search`, `fetch`, `search_wiki`, `fetch_wiki`, `sync_wiki_index`, `append_wiki_log`, `write_wiki_page`, `lint_wiki`, `reconcile_conflict`
diff --git a/changelog.md b/changelog.md
index 8becd5e..2d1669f 100644
--- a/changelog.md
+++ b/changelog.md
@@ -6,6 +6,30 @@
 
 Obsidian MCP 로컬 패키지(`mcp_obsidian`)의 workspace, code, docs, setup 흐름 변경을 기록한다.
 
+## 2026-04-12 — KG Dashboard UI Rule Alignment & Outlook Email Ontology Workbook Pipeline
+
+### Added
+
+- `app/services/outlook_email_workbook_contract.py` — Defines target sheet order and columns for the ontology workbook.
+- `app/services/outlook_email_raw_normalizer.py` — Normalizes raw email rows and extracts cross-references (case, site, lpo, phase, party).
+- `app/services/outlook_email_signal_extractor.py` — Extracts deterministic Document, Event, and Risk signals.
+- `app/services/outlook_email_ontology_mapper.py` — Maps signals into `canonical_ontology_export` statements.
+- `app/services/outlook_email_wiki_pointer_builder.py` — Generates `wiki_pointer` rows for Obsidian analysis links.
+- `scripts/build_outlook_email_ontology_workbook.py` — Orchestrates the pipeline to generate `output/spreadsheet/outlook_hvdc_email_ontology_workbook.xlsx`.
+- Added related test files in `tests/`.
+
+### Changed
+
+- `kg-dashboard/src/App.tsx`, `App.css` — Removed hero-style intro panel, simplified topbar, unified card radius to <=8px, removed negative letter spacing.
+- `kg-dashboard/src/components/GraphSidebar.tsx` — Cleaned up nested card-in-card patterns into flatter list/row structures.
+- `kg-dashboard/src/components/NodeInspector.tsx` — Simplified details grid into row/list-centric density structures.
+- `kg-dashboard/src/components/GraphView.tsx` — Simplified main graph frame to a functional viewport level.
+- `README.md`, `SYSTEM_ARCHITECTURE.md`, `LAYOUT.md` — Updated to reflect the new pipeline and dashboard UI alignment.
+
+### Verification
+- Verified dashboard tests, linting, and build pass cleanly without the hero panel.
+- Verified the Outlook email pipeline correctly extracts signals and generates the multi-sheet ontology workbook.
+
 ## 2026-04-12 — KG dashboard graph visibility docs sync
 
 ### Changed
